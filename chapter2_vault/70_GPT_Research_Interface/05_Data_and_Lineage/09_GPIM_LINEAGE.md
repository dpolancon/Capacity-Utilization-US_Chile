# GPIM Lineage

Investment flows
→ deflation
→ retirement/survival profile
→ seed/backcast
→ asset-specific productive stock
→ ME + NRC aggregate
→ validation and downstream authorization.

Working parameters:

- ME: \(L=14,\alpha=1.7,d=0.110\).
- NRC: \(L=30,\alpha=1.6,d=0.024\).

Verify current locked values before rerunning.
