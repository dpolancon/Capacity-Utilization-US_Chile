# Obsidian Integration

The repo contains an Obsidian vault or Markdown workspace.

Rules:

- treat `.obsidian/workspace.json` as interface noise;
- use YAML frontmatter for routing;
- keep one canonical note per decision or stage;
- use links for navigation, not duplicate copies;
- separate private daily notes from public research artifacts.
