# Common Repository Errors

- hard-coded user paths;
- silent overwrite of source files;
- output written to wrong stage;
- exploratory files consumed downstream;
- stale branch assumptions;
- missing validation;
- untracked provider handoff;
- stage names that do not match dependencies;
- local Obsidian noise mixed with scientific changes.
