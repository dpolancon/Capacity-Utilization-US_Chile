# Reproducibility Requirements

Scripts should:

- resolve repo root safely;
- use relative paths;
- document package versions;
- fail loudly on missing inputs;
- preserve raw data;
- write inspectable outputs;
- record source vintage;
- write validation and decision files;
- produce deterministic results where possible.
