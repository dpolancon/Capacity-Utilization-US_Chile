# Decision Dependencies

## Capital boundary chain

`ME + NRC`
→ GPIM inputs
→ capacity stock
→ \(k_t\)
→ interaction construction
→ coefficient meaning
→ \(\theta_t\)
→ capacity path
→ utilization
→ profitability denominator.

## Distribution chain

Wage-share definition
→ state variable
→ interaction term
→ transformation elasticity
→ capacity-growth contributions
→ interpretation of concavity.

## Anchor chain

Selected benchmark year
→ level constant of capacity
→ utilization level
→ profitability decomposition.

Changing any upstream decision requires revalidation downstream.
