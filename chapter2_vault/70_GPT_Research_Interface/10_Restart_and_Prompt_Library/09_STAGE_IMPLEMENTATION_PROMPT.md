# Stage Implementation Prompt

Implement one stage only.

Required:

- explicit inputs;
- relative paths;
- inspectable outputs;
- validation CSV;
- decision text;
- no source overwrite;
- no commit/push unless instructed.

Stop if upstream boundaries conflict.
